# Letterbox

A data science and API project using pandas, scikit-learn, and FastAPI.

## Setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

## Running

```bash
uvicorn main:app --reload
```
